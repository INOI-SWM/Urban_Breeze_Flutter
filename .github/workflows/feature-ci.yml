name: Feature CI

on:
  pull_request:
    branches:
      - develop

jobs:
  feature_ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Dart
        uses: dart-lang/setup-dart@v1

      - name: Install FVM
        run: dart pub global activate fvm

      - name: Cache pub & FVM SDK
        uses: actions/cache@v3
        with:
          path: |
            ~/.pub-cache
            .fvm/versions
          key: ${{ runner.os }}-fvm-${{ hashFiles('.fvmrc') }}
          restore-keys: |
            ${{ runner.os }}-fvm-

      - name: Check if FVM SDK is cached (for debugging)
        run: |
          if [ -d ".fvm/versions" ] && [ "$(ls -A .fvm/versions)" ]; then
            echo "FVM SDK directory exists and is not empty. Cache restored."
          else
            echo "FVM SDK directory is empty or does not exist. Will proceed with fvm install."
          fi

      - name: Install Flutter SDK via FVM
        env:
          FVM_USE_GIT_CACHE: false
        run: fvm install

      - name: Install dependencies
        run: fvm flutter pub get

      - name: Format check
        run: fvm dart format --set-exit-if-changed .

      - name: Analyze code
        run: fvm flutter analyze
